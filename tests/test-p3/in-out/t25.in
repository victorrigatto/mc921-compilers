int main() {
    int n, r, sum = 0, temp;
    print("enter the number = ");
    read(n);
    temp = n;
    while(n > 0) {
        r = n % 10;
        sum = (sum * 10) + r;
        n = n / 10;
    }
    if(temp == sum)
        print("palindrome number ");
    else
        print("not palindrome");
    return 0;
}